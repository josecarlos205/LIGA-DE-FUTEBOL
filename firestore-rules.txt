// Regras padrão para modo de teste do Firestore
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir leitura e escrita em todos os documentos durante o desenvolvimento
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
